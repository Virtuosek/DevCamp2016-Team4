<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BeeBack.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="using:LeSoir.Converters"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="BeeBack.Pages.MyActivitiesPage"
    mc:Ignorable="d">
    <Page.Resources>
        <Converters:IntPresenterConverter x:Key="IntPresenterConverter"/>
        <DataTemplate x:Key="ActivitiesDataTemplate">
            <Grid Width="200"
        		Height="200" 
        		Background="#FF18578B"
                  >
                <Interactivity:Interaction.Behaviors>
                    <Core:EventTriggerBehavior EventName="Tapped">
                        <Core:InvokeCommandAction Command="{Binding Selected, Mode=OneWay}"/>
                    </Core:EventTriggerBehavior>
                </Interactivity:Interaction.Behaviors>
                <StackPanel Margin="4">
                    <TextBlock Text="{Binding Title}"
        				TextWrapping="WrapWholeWords"
        				Foreground="White"
        				FontSize="16"
        				FontWeight="ExtraBold"/>
                    <TextBlock Text="{Binding Description}"
        				TextWrapping="WrapWholeWords"
        				Foreground="White"
        				FontWeight="Light"/>

                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Page.DataContext>
        <Binding Mode="OneWay" Path="MyActivitiesVM" Source="{StaticResource Locator}"/>
    </Page.DataContext>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding MyActivitiesList.Count, ConverterParameter=My Activities, Converter={StaticResource IntPresenterConverter}, Mode=OneWay}" FontSize="48"/>
        <GridView Grid.Row="1" 
			Margin="8"
			ItemsSource="{Binding MyActivitiesList}" 
			ItemTemplate="{StaticResource ActivitiesDataTemplate}"/>
        <StackPanel Orientation="Horizontal"
			Grid.Row="2">
            <Button Content="Add activity"/>

        </StackPanel>
    </Grid>
</Page>
